{
  "private": true,
  "name": "graziano-portfolio",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "URL_PATH=/ preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "URL_PATH=/ preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^7.17.0",
    "eslint-config-preact": "^1.1.3",
    "jest": "^27.4.7",
    "preact-cli": "^3.0.0",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "fflate": "^0.7.3",
    "history": "^5.2.0",
    "preact": "^10.1.0",
    "preact-markdown": "^2.1.0",
    "preact-router": "^4.0.1",
    "prismjs": "^1.26.0"
  }
}